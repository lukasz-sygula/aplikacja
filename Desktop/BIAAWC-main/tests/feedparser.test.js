const Feedparser = require('../src/utils/feedparser')


describe('Test the feed parser', () => {
  let feedParser

    beforeAll( () =>{
      feedParser = new Feedparser()
    });

    test('It should parse feed from url', () => {
    });

});